{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^6.5.1"
  },
  "scripts": {
    "lint": "lerna run lint",
    "postinstall": "lerna run deps:install && lerna link",
    "test": "lerna run test"
  }
}
