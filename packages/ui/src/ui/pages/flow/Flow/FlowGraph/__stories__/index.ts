import {composeStories} from '@storybook/react';

import * as FlowGraphStoryComponents from './FlowGraph.stories';

export const FlowGraphStories = composeStories(FlowGraphStoryComponents);

export default FlowGraphStories;
