import {composeStories} from '@storybook/react';

import * as AccountsSuggestStoryComponents from './AccountsSuggest.stories';

export const AccountsSuggestStories = composeStories(AccountsSuggestStoryComponents);

export default AccountsSuggestStoryComponents;
