import {RootState} from '../../reducers';

export const getIsEditing = (state: RootState) => state.dashboard2.dashboard2.isEditing;
export const getConfig = (state: RootState) => state.dashboard2.dashboard2.config;
