{
  "name": "tests",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "playwright test ${PW_OPTIONS}",
    "auth": "rm -rf ./.auth && playwright test -c ./playwright.auth.config.ts ${PW_AUTH_OPTIONS}",
    "screenshots": "E2E_MATCH=${E2E_MATCH:-.screen.} E2E_TEST_DIR=./screenshots playwright test ${PW_OPTIONS}"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "private": true,
  "devDependencies": {
    "@playwright/test": "1.53.1"
  }
}
