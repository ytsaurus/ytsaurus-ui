name: "[manual]: Add NDA description"

on:
  workflow_dispatch:
    inputs:
      prId:
        description: "Pr id"
        required: true
      url:
        description: "Url"
        required: true

jobs:
  docker-tag:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Node
        uses: actions/setup-node@v2
        with:
          node-version: "18.x"
          cache: "npm"
      - name: "Add link"
        run: |
          echo PR_ID=$PR_ID
          echo URL=$URL
          curl https://api.github.com/repos/ytsaurus/ytsaurus-ui/pulls/508 | jq .body | sed -E 's/<!-- nda-start -->.*<!-- nda-end -->/ /g' > body.txt
          node << __EOF
            const fs = require('fs')
            const body = fs.readFileSync('./body.txt');
            fs.writeFileSync('./new-body.json', JSON.stringify({
              body: [
                '<!-- nda-start -->$URL<!-- nda-end -->',
                body.slice(1, -1)
              ].join("\n")
            }))
          __EOF
          curl -L -X PATCH \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer <YOUR-TOKEN>" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            --data-binary @new-body.json \
            https://api.github.com/repos/OWNER/REPO/pulls/PULL_NUMBER \

        env:
          PR_ID: ${{ inputs.prId }}
          URL: ${{ inputs.url }}
